#include <stdio.h>

/**
 *main - entry point
 *
 * Return: 0 always
 */
int main(void)

	char i;
	char c;

	i = 65;
	c = 97
	if (i < 91)
	{
		putchar(i);
	}
	else
		if (c < 123)
			putchar(c);
		else break;
	return (0);
}

#include "main.h"

/**
 * print_last_digit - indicates if letter is caps or lower case
 * @i: is an arg
 * @a: is an arg
 * Return: last digit
 */

int print_last_digit(int i)
{

	int a, last = i % a;

	return (last);
}

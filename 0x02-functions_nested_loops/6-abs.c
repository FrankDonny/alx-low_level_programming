#include <stdlib.h>
#include "main.h"

/**
 * _isalpha - indicates if letter is caps or lower case
 * @c: is the arg
 * Return: 1 if the letter is lower, else returns 0
 */

int _abs(int)
{
	int a = abs();
	
	return (abs(int));
}

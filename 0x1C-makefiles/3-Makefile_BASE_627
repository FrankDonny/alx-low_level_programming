# -*- Makefile -*-

CC = gcc
SRC = main.c school.c
OBJ = *.o
NAME = school

all: $(NAME)

$(NAME): $(OBJ)
	$(CC) $(OBJ) -o $(NAME)

clean:
	rm -f *~ $(NAME)

oclean:
	rm $(OBJ[1]) $(OBJ[2])

fclean:
	rm -f *~ $(NAME)
	rm -f $(OBJ[1])
	rm -f $(OBJ[2])

re: fclean $(NAME)
